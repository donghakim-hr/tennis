# 남은 일 · 하지 않기로 한 것

## 후보 (효과가 큰 순서)

### 1. PWA — 홈 화면 설치 · 오프라인
코트장은 데이터가 약하다. `manifest.webmanifest` + 최소 service worker(`sw.js`)를 추가하면 홈 화면에 설치되고
오프라인에서도 열린다. 이미 `theme-color`, `apple-mobile-web-app-*`, SVG 파비콘은 들어가 있어 남은 건 두 파일이다.

주의: 서비스 워커 캐시는 **"파일 하나만 고쳐 push하면 반영"이라는 지금의 단순함을 깬다.**
버전 문자열을 두고 `activate`에서 옛 캐시를 지우는 처리를 반드시 함께 넣는다.

### 2. 결과 이미지(PNG) 저장
단톡방에서는 텍스트보다 이미지가 잘 읽힌다. 라이브러리 없이 `<canvas>`에 순위표·대진표를 직접 그려
`toBlob` → 다운로드/공유. 한글 폰트는 시스템 폰트를 그대로 쓰면 된다.

### 3. 조를 직접 지정하는 UI (고정 조)
지금은 **이름 순서로만** 조가 정해진다(사양서 2-7). 이름 카드에서 위/아래로 순서를 바꾸는 버튼이나
드래그가 있으면 편하다. 순서 변경은 `S.names`/`S.genders`/`S.levels`를 **같은 인덱스로 함께** 움직여야 한다.

### 4. 기록 탭 확장
- 여러 회차에 걸친 누적 순위(개인별 총 승/패·득실)
- 회차 검색·월별 묶기
- 기록 전체 내보내기/가져오기(JSON) — 기기를 바꿀 때 이동 수단이 없다

### 5. 4연속 출전 회피의 근본 개선
지금은 휴식자 선정에 연속 출전을 반영하는 휴리스틱이다(사양서 4). 코트가 꽉 차면 물리적으로 불가능하지만,
휴식이 1명 이상인 조합에서는 **라운드 순서를 나중에 재배열**하는 후처리로 더 줄일 여지가 있다.

### 6. 점수 입력 대안
현재는 숫자 키패드. 6-4, 7-5 같은 흔한 스코어를 원탭으로 넣는 프리셋 버튼이 있으면 빠르다.
단, 코트 카드 안이 이미 빡빡하므로 레이아웃 실측이 먼저다.

---

## 하지 않기로 한 것 (이유 있음 — 다시 제안하기 전에 읽을 것)

| 항목 | 이유 |
|---|---|
| **점수 이상치 경고**(60, 67 등) | 테니스는 타이브레이크 표기로 두 자리가 정당하다. 오탐만 늘고 신뢰가 깎인다. 포커스 시 전체 선택으로 원인(덧입력) 자체를 없앴다 |
| **이름 붙여넣기 일괄 분배** | `S.count`가 자동으로 바뀌면 코트 상한·성별 배열·고정 조 편성이 연쇄로 변하고 되돌릴 수단이 없다. '지난 명단 불러오기'가 같은 문제를 안전하게 해결한다 |
| **미완료 코트를 노란 테두리로** | `.court`의 3px 흰 테두리는 코트 라인 은유이고, 노랑은 이미 '승리·완료'(`.score.win`, `.rk.top`)를 뜻한다. 색 언어가 충돌한다 |
| **두 번째 회색 토큰(`--muted-cap`)** | 9.5~10.5px 라벨은 색을 더 어둡게 하기보다 **크기를 11px 이상으로 올리는** 편이 야외에서 효과적이다. 토큰이 늘면 '어느 회색을 쓸지' 결정 지점이 다시 생긴다 |
| **접기 토글을 카드 전체로 확장** | 펼친 상태에서 날짜 입력·칩·이름 칸 클릭이 버블링으로 카드를 닫는다. 머리글만 유지하고 접힌 상태에서 히트영역을 카드 여백까지 넓혔다 |
| **탭 좌우 화살표 키 내비게이션** | 한 손 스마트폰 사용 맥락에 키보드가 없다 |
| **`enterkeyhint`를 점수 입력의 주 해법으로** | `type=number` + `inputmode="numeric"`이면 iOS는 리턴 키 없는 숫자 패드를 띄운다. Android 한정 보조 이득이다 |
| **키보드 열릴 때 저장 바 숨김** | `focusout`이 칸 이동 사이에도 발생해 처리가 지저분해진다. 자동 저장을 넣어 저장 바를 누를 이유 자체를 없앴다 |
| **개인별 매트릭스 행 높이 확대** | 24라운드 매트릭스는 전체 조망용이다. 세로가 늘면 조망성이 떨어진다 |

---

## 구조적으로 남는 한계

- **실시간 공동 입력 불가** — 서버가 없다. 결과 열람은 공유 링크로 되지만, 링크로 본 사람이 점수를 입력해
  총무에게 되돌릴 수는 없다. 이걸 하려면 외부 DB/백엔드가 필요하고, 그 순간 "단일 HTML" 전제가 깨진다.
  (Firebase 등을 붙이려면 별도 브랜치에서, 오프라인 폴백을 유지한 채로 실험할 것)
- **기기 간 기록 이동 수단 없음** — `localStorage`는 기기·브라우저별로 분리된다. 위 4번(내보내기/가져오기)이 가장 값싼 해법이다.
- **공유 링크 길이** — 16명·6라운드는 링크가 길어진다(상한 8,000자). 일부 메신저가 긴 URL을 자를 수 있다.
