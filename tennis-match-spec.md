# 테니스 매칭 시스템 — 사양서

단일 HTML 파일(`tennis-match.html`)로 동작하는 테니스 대진 생성·결과 집계 도구.
PC 브라우저와 모바일에서 그대로 열리며, 외부 라이브러리나 서버가 필요 없다.

---

## 1. 요구사항 정리

| # | 요구사항 | 구현 상태 |
|---|---|---|
| 1 | 참가자 4~16명 | 완료 |
| 2 | 경기 수 임의 설정 (2~6게임) | 완료 — **1인당 경기 수** 기준 |
| 3 | 페어 중복 없이 최대한 많은 사람과 만나기 | 완료 |
| 4 | 참가자 이름 개별 입력 | 완료 |
| 5 | 득실 기준 실시간 순위 및 최종 결과 | 완료 |
| 6 | 웹·모바일 동시 확인 | 완료 — 반응형 단일 HTML |
| 7 | 코트 수 선택 | 완료 — 인원별 상한 적용 |
| 8 | 1라운드 첫 코트 고정 (1·2번 vs 3·4번) | 완료 |
| 9 | 4게임 연속 출전 회피 | 조건부 완료 — 아래 4-3 참조 |
| 10 | 일자별 결과 기록 | 완료 |
| 11 | 요약 대진표 | 완료 |
| 12 | 결과 저장 버튼 | 완료 |
| 13 | 진입 시 종목 선택 (단식 / 복식 → 동성 / 혼성) | 완료 |
| 14 | 참가자 입력 완료 시 설정 카드 접기 | 완료 — 5-1 참조 |
| 15 | 실력 등급으로 팀 전력 맞추기 | 완료 — 2-6 참조 |
| 16 | 혼성 복식 파트너 랜덤 / 고정 선택 | 완료 — 2-7 참조 |
| 17 | 링크로 결과 공유 (서버 없이) | 완료 — 5-8 참조 |
| 18 | 경기 중 한 손 조작 (점수 입력·라운드 추적) | 완료 — 5-2 참조 |
| 19 | 야외 가독성 · 터치 타깃 44px | 완료 — 10장 참조 |

---

## 2. 설정 항목

### 2-0. 종목 선택 (첫 진입)
처음 들어오면 대진 설정보다 먼저 종목을 고른다.

```
1단계   단식  /  복식
2단계   (복식 선택 시)  동성 복식  /  혼성 복식
```

| 종목 | 코트 1면 정원 | 팀 구성 | 성별 입력 |
|---|---|---|---|
| 단식 | 2명 | 1 대 1 | 없음 |
| 동성 복식 | 4명 | 2 대 2 | 없음 |
| 혼성 복식 | 4명 (남2·여2) | 남 1명 + 여 1명 | **필요** |

선택한 종목은 날짜 기록에 함께 저장된다. 설정 화면 맨 위 `종목` 카드의 `변경` 버튼으로 다시 고를 수 있고,
변경하면 현재 대진은 초기화된다(미저장 점수는 먼저 백업된다).

### 2-1. 경기 일자
`YYYY-MM-DD`. 대진을 만들면 이 날짜를 키로 기록이 저장된다.

### 2-2. 참가 인원
4~16명. 스테퍼(− / +)로 조절.

### 2-3. 코트 수
1~4면. 인원에 따라 선택 가능 범위가 제한된다.

**복식**

| 인원 | 선택 가능 코트 |
|---|---|
| 4~7명 | 1면 |
| 8~11명 | 1~2면 |
| 12~15명 | 1~3면 |
| 16명 | 1~4면 |

**단식** (코트당 2명이라 절반)

| 인원 | 선택 가능 코트 |
|---|---|
| 2~3명 | 1면 |
| 4~5명 | 1~2면 |
| 6~7명 | 1~3면 |
| 8명~ | 1~4면 |

- 상한 = `floor(인원 ÷ 코트정원)`, 최대 4
- 혼성 복식은 남녀 각각 `코트수 × 2`명이 필요하므로 `floor(남÷2)`, `floor(여÷2)`로도 제한된다
- 사용자가 코트를 직접 고르기 전까지는 인원에 맞는 최대값이 자동 적용된다
- 인원을 줄여 상한이 낮아지면 코트 수도 자동으로 내려간다

### 2-4. 1인당 경기 수
2~6경기. **라운드 수가 아니라 개인이 뛰는 경기 수**다.

```
단식 · 동성 복식   필요 라운드 = ⌈인원 × 1인당 경기수 ÷ (코트수 × 코트정원)⌉
혼성 복식          필요 라운드 = ⌈1인당 경기수 × max(남, 여) ÷ (코트수 × 2)⌉
```

인원이 `코트수 × 4`로 나누어떨어지지 않으면 라운드가 늘어난다.
예) 7명 · 1코트 · 인당 4경기 → **7라운드**

슬롯이 정확히 나누어떨어지지 않으면 일부 인원이 1경기 더 뛴다. 설정 화면에
`총 5라운드 진행 · 전원 4경기 (4명은 5경기)` 형태로 미리 표시된다.

### 2-5. 참가자 이름
개별 입력(최대 10자). 비워두면 `참가자 N`으로 표시된다.
혼성 복식에서는 이름 칸 오른쪽에 `남`/`여` 토글이 붙는다. 기본값은 남·여 번갈아.
실력 등급을 쓰면 `A`/`B`/`C` 버튼도 함께 붙는다(누르면 A → B → C 순환).

### 2-6. 실력 등급
`사용 안 함` / `등급으로 전력 맞추기` 중 선택. 기본은 사용 안 함.

| 등급 | 내부 값 |
|---|---|
| A (상) | 3 |
| B (중, 기본) | 2 |
| C (하) | 1 |

켜면 대진을 짤 때 **양 팀 등급 합의 차이**를 비용에 넣어 전력이 비슷한 팀끼리 붙게 한다.
중복 회피가 여전히 1순위이므로 편차가 항상 0이 되지는 않는다(3-2 가중치 참조).
켜져 있으면 대진표·요약표 이름 옆에 등급이 작게 표시되고, 대진표 상단에 `팀 전력 편차 평균 0.6`이 붙는다.

### 2-7. 파트너 (혼성 복식 전용)

| 선택 | 동작 |
|---|---|
| `랜덤` (기본) | 매 경기 새 파트너. 같은 사람과 두 번 짝이 되지 않도록 배분 |
| `고정 조` | 이름 순서대로 2명씩 한 조가 되어 끝까지 같은 파트너로 뛴다 |

고정 조는 **이름 순서**로 정해진다 — (1·2), (3·4), (5·6) … 조 편성을 바꾸려면 이름 순서를 바꾼다.
설정 화면에 `1조 · 홍길동 · 김영희` 형태로 편성이 미리 표시된다.

조건을 만족해야 쓸 수 있고, 어긋나면 그 사유가 표시되며 `대진표 만들기`가 잠긴다.

- 인원이 짝수
- 각 조가 남 1명 · 여 1명 (성별 토글 순서가 남·여·남·여여야 자동으로 맞는다)
- 조가 2개 이상

고정 조에서는 코트 1면에 조 2개가 필요하므로 **선택 가능 코트 = `floor(조 수 ÷ 2)`** 로 더 제한된다.
필요 라운드도 조 단위로 계산한다.

```
필요 라운드 = ⌈조 수 × 1인당 경기수 ÷ (코트수 × 2)⌉
```

---

## 3. 대진 생성 알고리즘

### 3-1. 기본 구조
라운드마다 다음을 반복한다.

1. **출전자 선정** — 출전 횟수가 적은 사람 우선, 동점자는 연속 출전이 긴 사람을 뒤로 밀어 휴식 배정
2. **코트 배정** — 종목별로 다르다
   - 단식: 2명씩 묶어 그대로 대진
   - 동성 복식: 4명씩 묶고 3가지 팀 조합 중 최저 비용 선택
   - 혼성 복식: 남녀 풀을 따로 관리해 코트마다 남 2·여 2를 배정, 남녀 짝짓는 2가지 중 최저 비용 선택
3. **300회 재시도** 후 최저 비용 조합 채택, 비용 0이면 즉시 확정
4. 전체 스케줄을 **10회 생성**해 최종 선택

### 3-2. 비용 함수

| 항목 | 가중치 |
|---|---|
| 같은 사람과 다시 페어 | 40 |
| 같은 사람과 다시 상대 | 4 |
| 팀 등급 합 1점 차이 (등급 사용 시) | 12 |
| 4연속 출전 발생 인원 (스케줄 단위) | 60 |

페어 중복을 상대 중복보다 10배 무겁게 취급한다.
등급을 켜도 **중복 회피가 우선**이다(페어 중복 1회 = 40 > 등급 3점 차 = 36).

### 3-2-1. 파트너 고정(고정 조) 대진
조가 처음부터 고정이므로 페어 중복이라는 개념이 없다. 조 단위로만 배정한다.

1. 출전 조 선정 — 출전 횟수가 적은 조 우선, 동점이면 연속 출전이 긴 조를 뒤로
2. 남은 조를 짝지어 코트에 배정, **300회 재시도** 후 최저 비용 채택
3. 비용 = `같은 조와 다시 붙음 × 8` + `조 등급 합 차이 × 12`

휴식은 조 단위로 발생하므로 한 조(2명)가 함께 쉰다.

### 3-3. 고정 규칙 — 1라운드 코트 1
- 복식: **1·2번 vs 3·4번**
- 단식: **1번 vs 2번**

해당 인원은 1라운드에 반드시 출전한다. 이 조합은 기록에 반영되므로 이후 라운드에서 다시 나오지 않는다.

혼성 복식에서 1~4번이 남녀 한 쌍씩이 아니면(예: 1·2번이 둘 다 남) 팀을 만들 수 없으므로 고정이 자동 해제되고,
설정 화면에 그 사실이 안내된다. 순서를 남·여·남·여로 맞추면 적용된다.

### 3-4. 보장 사항

- 목표 경기 수 **미달자 0명**
- 출전 횟수 편차 **최대 1경기** (혼성 복식은 성별 내에서 최대 1경기. 남녀 인원이 다르면 적은 쪽이 더 많이 뛴다)
- 생성 코트 수·라운드 수가 설정값과 일치
- 혼성 복식의 모든 팀은 남 1명 · 여 1명
- 파트너 고정에서는 모든 팀이 설정 화면에 표시된 조 편성 그대로 (출전 편차는 조 단위로 최대 1경기)

---

## 4. 4연속 출전 회피 규칙

### 4-1. 적용 조건
**인원 6명 이상 + 1인당 4경기 이상**일 때만 작동한다.

### 4-2. 동작
휴식자를 고를 때 1순위(출전 횟수)는 그대로 두고, **동점자 중 연속 출전이 긴 사람을 먼저 쉬게** 한다.
출전 횟수 기준을 건드리지 않으므로 목표 경기 수와 편차 보장이 유지된다.

### 4-3. 수학적 한계
4라운드 창 안에서 전원이 최소 1번 쉬려면 다음을 만족해야 한다.

```
라운드당 휴식 인원 × 4 ≥ 대상 인원
휴식 인원 = 인원 − 코트수 × 코트정원
```

혼성 복식은 남녀 각각에 대해 이 조건을 만족해야 한다.

코트가 꽉 차는 조합(휴식 0명)에서는 **물리적으로 불가능**하다.
코트 수를 줄이면 대부분 해결된다.

| 인원 | 코트 | 회피 | 최장 연속 |
|---|---|---|---|
| 8명 | 2면 | 불가능 | 4경기 |
| 8명 | 1면 | **가능** | 1경기 |
| 12명 | 3면 | 불가능 | 4경기 |
| 12명 | 2면 | **가능** | 2경기 |
| 16명 | 4면 | 불가능 | 4경기 |
| 16명 | 3면 | **가능** | 3경기 |

대신 라운드 수가 늘어난다. 16명·인당 4경기 기준 4코트는 4라운드, 3코트는 6라운드.

회피 불가 조합에서는 발생 인원을 최소화하고, 대진표 상단에 빨간 안내문을 띄운다.

---

## 5. 화면 구성

### 5-1. 설정 화면
종목 → 경기 일자 → 참가 인원 → 코트 수 → 1인당 경기 수 → 파트너(혼복만) → 실력 등급 → 참가자 이름 → `대진표 만들기`

**접이식 설정 카드**
카드는 **언제나** 접거나 펼칠 수 있다. 이미 정해진 값(종목·일자·파트너·실력 등급)은 **처음부터 접힌 상태**로 시작하고,
직접 정해야 하는 값(인원·코트 수·경기 수·이름)은 펼쳐져 있다.
참가자 이름을 전원 입력하고 **포커스가 설정 화면을 벗어나면** 나머지도 1회 자동으로 접힌다.
접힌 카드는 머리글에 현재 값을 요약해 보여준다.

```
종목           복식 · 혼성        ›
경기 일자      2026-07-31        ›
참가 인원      8명               ›
코트 수        코트 2면          ›
1인당 경기 수  1인당 4경기        ›
파트너         고정 조            ›
실력 등급      A 3 · B 3 · C 2   ›
참가자 이름    선수1, 선수2, …    ›
```

- 머리글(높이 44px, 카드 폭 전체)을 누르거나 포커스 후 Enter·Space로 개별 카드를 펼치고 접는다
- 카드 목록 위 `설정 모두 접기` / `설정 모두 펼치기` 버튼으로 한 번에 처리
- 자동 접기는 **이름 칸 사이 이동에는 반응하지 않는다.** 포커스가 `#setup` 밖으로 나갔을 때만,
  그것도 다음 틱으로 미뤄서 접는다 (접히는 도중 좌표가 밀려 `대진표 만들기` 탭이 빗나가던 문제)
- 손으로 펼친 카드는 이름을 수정해도 다시 접히지 않는다

**하단 고정 CTA**
설정 화면에는 저장 바와 같은 형태의 스티키 바가 붙는다. 카드 8장을 지나 스크롤하지 않아도 대진을 만들 수 있다.

```
8명 · 코트 2면 · 4라운드 · 1인당 4경기        [ 대진표 만들기 ]
```

설정에 문제가 있으면(혼복 성별 부족, 고정 조 조건 미충족) `설정을 확인해 주세요`로 바뀌고 버튼이 잠긴다.

**지난 명단 불러오기**
참가자 이름 카드 상단에 `7월 31일 (금) 명단 불러오기 (8명)` 버튼이 붙는다.
지난 회차의 이름·성별·등급·인원을 그대로 가져온다. 동호회 반복 사용에서 이름 16개 재입력을 없앤다.

### 5-2. 대진표 탭
- 코트 모양 카드: 위쪽 A팀, 가운데 흰 네트 선, 아래쪽 B팀
- 이긴 팀 점수 칸은 테니스공 색으로 강조
- 상단 요약: `복식 · 동성 · 10명 · 1인당 4경기 · 5라운드 · 코트 2면 · 중복 페어 없음 · 최장 연속 3경기`
  (단식은 `중복 페어` 대신 `중복 대진`으로 표기.
  고정 조는 `파트너 고정`이 붙고 중복 페어 항목이 빠진다. 등급 사용 시 `팀 전력 편차 평균 0.6`이 붙는다)
- 등급 사용 시 이름 옆에 `A`·`B`·`C`가 작게 표시된다
- 휴식 인원은 라운드 하단에 표시
- **하단 고정 저장 바** (5-6 참조)

**경기 중 조작 (한 손 · 스마트폰 기준)**

| 동작 | 처리 |
|---|---|
| 라운드 위치 | 라운드 헤더가 앱 헤더 바로 아래에 **고정(sticky)**된다. 기준점 `--hh`는 탭 바 유무에 따라 런타임 측정 |
| 라운드 진행도 | 헤더 오른쪽에 `2 / 3` 배지. 다 차면 녹색. 점수 입력 시 전체 재렌더 없이 즉시 갱신 |
| 점수 칸 진입 | 코트 카드의 **어디를 눌러도** 그 팀 점수 칸으로 포커스가 간다 (이름 영역이 행 면적의 4/5인데 무반응이었다) |
| 기존 점수 수정 | 포커스 시 **전체 선택**. `6`이 든 칸에 `7`을 치면 `67`로 저장되던 문제를 없앤다 |
| 저장 | 입력이 멈추면 **700ms 후 자동 저장**. 저장 바는 상태 표시가 되고 버튼은 `지금 저장`으로 남는다 |
| 탭 왕복 | 탭별 스크롤 위치를 기억한다. 순위를 보고 돌아오면 보던 코트 그대로 |
| 토스트 | 저장 바(약 72px) 위쪽에 뜬다 (가리던 문제) |

### 5-3. 요약 탭
| 표 | 내용 |
|---|---|
| 라운드별 대진 | 행=라운드, 열=코트. `김·이 6:4 박·최` 형태, 승리 팀 녹색 |
| 개인별 출전 코트 | 행=참가자, 열=라운드. `2A`=2번 코트 A팀, 코트별 색상 구분, 휴식은 회색 `휴` |

개인별 표에는 **경기**(총 출전 수)와 **연속**(최장 연속 출전) 열이 있고, 연속이 4 이상이면 빨간색.

### 5-4. 순위 탭
| 열 | 내용 |
|---|---|
| # | 순위 (동점 공동 순위) |
| 이름 / 경기 / 승 / 무 / 패 / 득 / 실 / 득실 | 집계값 |

- 기본 정렬: **득실 우선** → 승 → 득점
- 토글: **승수 우선** → 득실 → 득점
- 1~3위는 테니스공 색 배지

### 5-5. 기록 탭
날짜별 목록. 각 항목에 `12명 · 코트 2면 · 6라운드 · 1인당 4경기 · 8/12경기 입력 · 1위 홍길동` 표시.
탭하면 그날 대진과 점수가 그대로 열린다. 개별 삭제 가능.

### 5-6. 결과 저장 바
대진표 탭 하단에 고정.

| 상황 | 버튼 | 문구 |
|---|---|---|
| 점수 입력 전 | 비활성 `지금 저장` | 점수를 입력하면 자동으로 저장됩니다 |
| 입력 직후 | 파란색 `지금 저장` | 3/10 경기 · 입력 중 (자동 저장됩니다) |
| 저장 완료 | 초록색 `저장됨` | 3/10 경기 · 14:32 저장 |

점수는 **입력이 멈추면 700ms 뒤 스스로 저장된다.** 버튼은 안심용 수동 경로로 남는다.
앱을 벗어날 때도 미저장 점수를 백업한다(`pagehide`, `visibilitychange`).

### 5-7. 하단 버튼
`공유 링크 복사` · `결과 복사` · `새 경기 시작` · `대진 다시 짜기`

안전한 동작을 앞에, **파괴적 동작(`대진 다시 짜기`)을 맨 뒤에** 두고 붉은 테두리로 구분한다.
`새 경기 시작`은 점수가 입력된 상태에서 `입력한 점수는 (날짜) 기록에 저장됩니다. 새 경기 설정으로 돌아갈까요?`로 확인한다
(점수는 실제로 사라지지 않고 그 날짜 기록에 남으므로, 겁주는 문구를 쓰지 않는다).

`결과 복사`는 날짜·설정·전체 대진·점수·순위를 텍스트로 만들어 클립보드에 넣는다. 단톡방 공유용.

### 5-8. 공유 링크
`공유 링크 복사`는 현재 대진과 점수를 **URL 해시에 담은 링크**를 만든다.
`navigator.share`가 있으면 공유 시트를 띄우고, 없으면 클립보드에 복사한다.

```
https://<주소>/#v1=<base64url(설정~이름~대진·점수)>
```

- 서버가 없어도 링크를 받은 사람이 **그날 대진·점수·순위를 그대로** 본다
- 휴식자는 담지 않고 복원 시 다시 계산한다(그만큼 링크가 짧아진다)
- 12명·4라운드 기준 약 470자. 8,000자를 넘으면 만들지 않고 안내한다

링크로 열면 **읽기 전용**이다.

| | 동작 |
|---|---|
| 헤더 날짜 칩 | `열람용 · 7월 31일 (금)` — 붉은 칩으로 **모든 탭에서 상시** 표시 |
| 점수 칸 | `readonly` + 점선 테두리. 누르면 `공유받은 결과입니다. 점수는 수정할 수 없습니다` 토스트 |
| 저장 바 · `대진 다시 짜기` · `새 경기 시작` | 숨김 |
| 대진표 상단 | 안내 카드 + `내 기록에 저장하고 점수 수정하기` 버튼 |
| 요약 · 순위 탭 | 같은 고지 1줄 |

`내 기록에 저장`은 **항상 확인을 받는다**(같은 날짜 기록이 있으면 덮어쓰기 경고). 저장 후에는
`기록 탭에서 삭제할 수 있습니다`를 함께 안내해 되돌리는 방법을 알려준다.
`새 경기 시작` · `종목 변경` · `대진표 만들기` · 기록 열기는 열람 중이면 먼저 확인을 받고, 읽기 전용을 해제하며 해시를 지운다.

**링크가 깨진 경우**
메신저가 긴 URL을 자르면 디코딩이 실패한다. 이때 **내 옛 기록을 남의 결과로 오인하지 않도록**
공유 해시가 있으면 로컬 세션을 아예 복원하지 않고, 종목 선택 화면에 안내를 띄운다.

```
공유 링크를 읽을 수 없습니다. 메신저가 긴 주소를 잘랐을 수 있어요 —
단톡방에서 링크 전체를 다시 복사해 붙여주세요.
```

**이미 앱이 열려 있을 때**
같은 주소의 해시만 바뀌면 브라우저는 새로고침하지 않는다. `hashchange`를 받아
입력 중이던 점수를 먼저 저장한 뒤 공유받은 결과로 전환한다.

---

## 6. 순위 계산

경기당 양팀 점수를 입력하면 즉시 반영된다.

- **승/무/패** — 양쪽 점수가 모두 입력된 경기만 집계
- **득점** = 소속 팀 점수 합 / **실점** = 상대 팀 점수 합
- **득실** = 득점 − 실점
- 동점자는 공동 순위

---

## 7. 데이터 저장

```
tennis:sessions
└─ { sessions: { "2026-07-31": {...}, "2026-08-02": {...} }, current: "2026-08-02" }
```

세션 스냅샷: `date, mode, pair, genders[], count, courts, games, names[], levels[], useLevel, mixPartner, schedule[], sort, savedAt`

구버전 데이터에 `levels` · `useLevel` · `mixPartner`가 없으면 각각 전원 B · 미사용 · 랜덤으로 읽는다.

저장 위치를 순서대로 시도한다.

1. `window.storage` — Claude 아티팩트 미리보기 환경
2. `localStorage` — 일반 브라우저(직접 호스팅·파일 열기). **기기·브라우저별로 따로 저장**된다
3. 메모리 — 위 둘 다 막힌 경우(시크릿 모드 등). 새로고침 시 사라지며, 설정 화면에 경고가 표시된다

서버가 없으므로 **실시간 공동 입력은 되지 않는다.** 총무 한 명이 입력하고, 결과는 `공유 링크`(5-8)나 `결과 복사`로 나눈다.

---

## 8. 검증 결과

jsdom으로 실제 화면을 조작해
**단식 / 동성 / 혼성(랜덤) / 혼성(고정 조) × 등급 사용·미사용 × 인원 4~16명 × 선택 가능 코트 × 1인당 2·4·6경기
= 708개 조합**을 생성하고 매번 아래를 검사했다. 위반 0건, 런타임 오류 0건.

- 생성된 코트 수·라운드 수가 설정과 일치
- 1라운드 코트 1 = 1·2번 vs 3·4번 고정 유지 (단식은 1번 vs 2번)
- 목표 경기 수 미달자 0명
- 출전 횟수 편차 ≤ 1경기 (혼복은 성별 내, 고정 조는 조 내)
- 혼성 복식의 모든 팀이 남 1명 · 여 1명
- 고정 조의 모든 팀이 (1·2), (3·4) … 편성 그대로
- 회피 가능 조건에서 4연속 발생 0건
- 표본 전 구간 중복 페어 0회

등급 사용 시 팀 전력 편차(양 팀 등급 합 차이)의 평균은 3회 평균으로 아래와 같이 줄었다.

| 조합 | 등급 미사용 | 등급 사용 |
|---|---|---|
| 동성 12명 3코트 4경기 | 1.56 | **0.56** |
| 단식 8명 2코트 4경기 | 1.04 | **0.59** |
| 혼성(랜덤) 12명 3코트 4경기 | 1.17 | **0.61** |
| 혼성(고정 조) 12명 3코트 4경기 | 1.28 | **0.67** |

공유 링크는 인코딩 → 새 브라우저 컨텍스트에서 디코딩까지 왕복 검사해 대진·점수·휴식이 완전히 일치함을 확인했다.

**UI/UX 개선 검증** — jsdom으로 8개 영역 40여 항목을 자동 검사했다(접힘 기본 상태, 자동 접기 타이밍,
하단 CTA, 점수 칸 포커스·라운드 배지·자동 저장, 탭·ARIA, 코트 수 자동 선택, 등급 안내, 명단 불러오기, 깨진 공유 링크).

추가로 **실제 Chromium(배포본)에서 직접 조작해** 다음을 확인했다.

| 항목 | 실측 |
|---|---|
| `[hidden]` 적용 | `hidden` + `.setup` → `display:none` (수정 전 `grid`) |
| 종목 선택 화면 문서 높이 | 1,362px → **812px** (뷰포트와 동일) |
| 라운드 헤더 고정 | 스크롤 871px 시점에 `2라운드 0/2`가 `top=87px`(앱 헤더 높이)에 고정 |
| 탭 스크롤 기억 | 대진표 871px → 순위 0px → 대진표 복귀 **871px** |
| 터치 타깃 | 접이식 머리글 345×44 · 인원 스테퍼 44×44 · 코트 칩 52×45 · CTA 132×51 |
| 코트 카드 탭 | 이름 영역을 눌러 점수 칸으로 포커스 이동 |
| 자동 저장 | 버튼을 누르지 않고 `1/8 경기 · 17:23 저장` |
| 열람용 | 붉은 `열람용 ·` 칩, 점선 readonly, 안내 카드 + 저장 버튼, 요약·순위 고지, 탭 시 토스트 |

UI는 전 흐름(설정 → 접기 → 생성 → 점수 입력 → 저장 → 탭 이동 → 공유 → 읽기 전용 열람 → 내 기록에 저장 → 새 경기 → 기록 재오픈 → 삭제)을 자동 실행해 런타임 오류가 없음을 확인했다.

---

## 9. 시각 설계 · 접근성 기준

### 9-1. `[hidden]` 무력화 문제 (실제로 있었던 버그)
`.setup{display:grid}` 같은 **저자 선언은 UA 스타일시트의 `[hidden]{display:none}`을 캐스케이드에서 이긴다**
(Chrome·Safari 기준. Firefox는 `!important`라 영향 없음). 그래서 `hidden` 속성을 켜도 설정 폼·탭 바·하단 버튼이
계속 렌더되어, 첫 화면 1,362px 중 853px이 '숨겨진' 폼이었고 대진표를 만들어도 최상단에 폼이 남아
대진표가 850px 아래로 밀려났다.

```css
[hidden]{display:none!important}
```

이 한 줄로 해결. 첫 화면 문서 높이가 **1,362px → 812px**(뷰포트와 동일)로 줄었다.

### 9-2. 색 대비 (WCAG AA 4.5:1)
`--muted`를 `#6d7c8a` → **`#5a6875`**로 내렸다. 이 토큰은 힌트·표 헤더·탭 라벨·저장 상태 등 **33곳**에 쓰인다.

| 조합 | 이전 | 이후 |
|---|---|---|
| `--muted` on `--card` | 4.29:1 ✗ | **5.7:1** ✓ |
| `--muted` on `--bg` | 3.78:1 ✗ | **5.0:1** ✓ |

코트 카드 안의 흰색 알파 텍스트도 함께 올렸다.

| 요소 | 이전 | 이후 |
|---|---|---|
| `.court-label` | 10px / .72 (3.7:1) | 11px / .95 |
| `.players .sub` | 10.5px / .6 (3.0:1) | 11.5px / .88 |
| `.score::placeholder` | .4 (2.1:1) | .8 |
| `.score` 테두리 | 1px / .35 (2.2:1) | **2px / .6** (3.2:1, 1.4.11 충족) |

최소 폰트 크기도 올렸다 — 표 헤더 10 → 11.5px, 등급 배지 9.5 → 11px, 진행 카운터 12 → 13px, 저장 상태 12.5 → 13.5px.
비활성 저장 버튼은 배경을 `--hair` → `--card`로 바꿔 라벨 대비(3.36:1)를 확보했다.

### 9-3. 터치 타깃 (최소 44×44)

| 요소 | 이전 | 이후 |
|---|---|---|
| 성별 토글 `.gbtn` | 30×29 | **44×44** |
| 등급 버튼 `.lvbtn` | 26×29 | **44×44** |
| 접이식 머리글 | 폭 전체 × 19.5 | **345×44** (카드 폭 전체) |
| `설정 모두 접기` | 38.75 | **48** |
| 탭 | 27×31 | **44** + 선택 탭 배경 강조 |
| 기록 삭제 | 40×34, hover에만 붉은색 | **44×44**, 상시 붉은색 |

### 9-4. 그 밖
- 요약·순위 표의 **첫 열 고정**(sticky). 가로로 밀어도 라운드 번호·이름이 남는다
- 가로 스크롤 안내는 **실제로 넘칠 때만** 붙인다(`scrollWidth > clientWidth`, 숨은 탭은 폭 0이라 건너뜀)
- 탭에 `role="tablist"`/`role="tab"`/`aria-controls`, 패널에 `role="tabpanel"`, 진행 카운터에 `aria-live="polite"`

---

## 10. 알려진 제약

1. **4연속 출전** — 코트가 꽉 차는 조합에서는 회피 불가 (4-3 참조)
2. **혼성 복식 성별 불균형** — 남녀 인원이 다르면 적은 쪽이 더 많은 경기를 뛴다. 균등하게 하려면 남녀 인원을 맞춰야 한다
3. **중복 페어** — 4~7명이 5~6경기를 뛰면 조합 자체가 부족해 중복 발생. 4명이면 가능한 페어가 3가지뿐이라 6경기 시 최소 6회 중복
4. **등급 편차 0은 보장하지 않는다** — 중복 회피가 우선이고, 출전 인원이 라운드마다 거의 고정되는 조합(전원 매 라운드 출전 등)에서는 고를 수 있는 팀 조합 자체가 적다. 등급이 3단계뿐인 것도 한계
5. **고정 조 편성은 이름 순서로만** — 조를 임의로 지정하는 UI는 없다. 이름 순서를 바꿔서 편성한다
6. **실시간 공동 입력 불가** — 기기별 로컬 저장이다. 결과 열람은 공유 링크(5-8)로 되지만, 링크로 본 사람이 점수를 입력해 되돌릴 수는 없다. 필요하면 서버나 외부 DB 연동이 필요
7. **공유 링크 길이** — 16명·6라운드처럼 큰 대진은 링크가 길어진다(상한 8,000자). 일부 메신저가 긴 URL을 자를 수 있다
